### 角色：
[填写你的角色，如：前端高级开发工程师、后端Java开发工程师、测试工程师等]

### 模块：
[填写涉及的模块或文件，如：CommonIframe.vue、UserService.java、支付模块]

### 问题：
[清晰描述 Bug 现象，越具体越好]
- 复现场景：在什么操作下出现？
- 表现形式：页面报错？功能失效？数据异常？
- 影响范围：影响哪些用户或功能？

### 过程：
[按时间顺序描述复现步骤，最好分点列出]
1. 第一步操作：[具体操作，如：点击“打开iframe”按钮]
2. 第二步操作：[具体操作，如：切换到其他模块]
3. 第三步操作：[具体操作，如：点击浏览器返回按钮]
4. 复现结果：[出现的问题，如：页面提示“地址不存在”]

### 预期结果：
[描述修复后应该达到的正确行为]
- 操作后应该出现什么？
- 页面状态、数据、交互应该如何？

### 根因分析：
[分析问题产生的核心原因，可从代码、逻辑、依赖等角度]
1. 原因一：[如：路由参数未写入历史栈，导致返回时丢失]
2. 原因二：[如：未监听路由变化，iframe src 未更新]
3. 原因三：[如：状态仅存在内存，未持久化到路由或全局 store]

### 修复方案：
[列出具体的修复步骤和代码修改点]
1. 方案一：[如：将 iframe 地址作为路由 query 参数传递]
2. 方案二：[如：在 CommonIframe.vue 中添加 $route 监听]
3. 代码修改：
   - 文件：[如：src/views/CommonIframe.vue]
   - 关键函数：[如：watch 监听 $route，mounted 中添加 popstate 监听]
   - 核心逻辑：[如：每次路由变化时，更新 iframe 的 src 属性]
4. 如果不确定要怎么改，就让AI去检查判断   

### 测试验证：
[描述如何验证修复是否成功]
1. 测试步骤：[如：重复“过程”中的操作]
2. 预期结果：[如：返回后页面正常加载，地址栏参数完整]
3. 边界情况：[如：刷新页面、多次返回、多模块切换是否正常]